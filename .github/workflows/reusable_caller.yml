name: Reusable caller workflow
on: workflow_dispatch
jobs:
  use-case-1:
        uses: ./.github/workflows/reusable_workflow.yml
        with:
         name: Germa-bikear-end
  use-case-2:
        uses: ./.github/workflows/reusable_workflow.yml
        with:
          name: Notty-dotty
          car: FZ1
  use-case-3:
        uses: ./.github/workflows/reusable_workflow.yml
        with:
          name: Notty-dotty
          car: Ferrari-91
